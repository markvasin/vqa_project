optimizer:
  type: adam_w
  params:
    lr: 5e-5
    eps: 1e-8

evaluation:
  metrics:
  - accuracy

training:
  batch_size: 64
  snapshot_interval: 6000
  early_stop:
    criteria: clevr/accuracy
    minimize: false
